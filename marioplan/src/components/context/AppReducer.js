const AppReducer = (state, action) =>{
        switch(action.type) {
            case 'CREATE_PROJECT' : 
            return state
            default :
            
        }
}


export default AppReducer;